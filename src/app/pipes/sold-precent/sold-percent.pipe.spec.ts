import { SoldPercentPipe } from './sold-percent.pipe';

describe('SoldPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new SoldPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
